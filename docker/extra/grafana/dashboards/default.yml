apiVersion: 1

providers:
  - name: Inkless Kafka # A uniquely identifiable name for the provider
    folder: kafka-inkless # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards/data